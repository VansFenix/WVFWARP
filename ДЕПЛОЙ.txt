=== ДЕПЛОЙ НА VERCEL + SUPABASE ===

Строка подключения уже готова — файл БД.txt

1. Задеплоить на Vercel:
   - https://vercel.com → Add New Project → VansFenix/WVFWARP
   - В Environment Variables добавить:
     DATABASE_URL = строка из БД.txt (всё вместе, включая DATABASE_URL=)
   - Deploy

2. Накатить схему БД (Vercel сам выполнит при деплое через drizzle-kit push)

Всё. Сайт будет работать.
